# DDL Command Center · 项目亮点

> 面向北大学生的 **DDL 作战指挥中心**：教学网作业、课程表、考试日历、个人 TODO、AI 助手——整合到一个本地桌面应用。

---

## 我们解决了什么真痛点

**1. 教学网作业逐条手抄太烦**
一键登录教学网（IAAA SSO），全部待办自动同步进库。课表也支持从北大门户 HTML 一次导入，连期末考试场次都一并提取。

**2. 同步会不会覆盖我手动改过的任务？**
**不会。** 我们做了契约级保证：同步命中已存在的任务直接跳过，标题、备注、用户编辑过的字段一律不动。专门有一条单元测试锁住这个行为，防止以后被误改回。

**3. 教学网里很多老学期作业、无截止时间的公告混着真任务**
做了**三桶分流**：已逾期的丢；有 due 的入库；没 due 的交给 AI 兜底解析。每条数据为啥被丢都有独立计数器，便于排查。

**4. 自然语言一句话录入**
"明天下午 3 点开组会，理科楼 206" → 自动判断是任务/课程/考试并填好所有字段。AI 不可用时，本地 regex fallback 也能处理 80% 常见说法（含"24 点交报告"、"下周一" vs "这周一"等坑）。

**5. 直观的"现在"和"DDL"提示**
课表上画两条横线：**红色 = DDL**，**蓝色 = 当前时间**。一眼看到这周哪天最紧。

**6. 个性化设置**
4 套主题（PKU 红 / **THU 紫** / 海蓝 / 深色）、4 档字号、自定义头像和显示名——都本地存储，不会被 push 到云端影响别人。

---

## 几个我们觉得做得有意思的点

### AI 成本控制：三道闸

DeepSeek 是按 token 收费的。我们设了：
- **每日 token 预算**：发请求**前**用 `len/4` 估算 token 数，超预算直接拦截
- **摘要缓存**：每条 DDL 描述按 SHA256 缓存 1 小时——同一个 DDL 在多次同步里只算一次钱
- **短跳过**：描述 ≤ 120 字直接走截断，不调 AI

> 实际效果：日常同步 + 聊天的 token 消耗稳定在每天几千以内。

### AI 看到的是「裁剪过的世界」

聊天里问"我下周三有空吗"，光发用户消息没用——得让 AI 看到任务+课表。但全发又烧 token。
我们每轮注入精心裁剪过的上下文：开放任务排序后取 8-12 条；课表只取当前周；超过 80 个时段自动折叠成 `其余 N 个已省略`。
答得准，单轮 prompt 也有上限。

### 主题切换不动 50 处样式表

工程里 50+ 处样式表硬写了 `PKU_RED` 这个名字。要加 THU 紫、海蓝、深色三个主题，**改 50 个文件不现实**。
做法：色名不动，只换色值——每个主题里 `PKU_RED` 仍然是"主色槽位"，只是它的实际值变成了清华紫 `#660874`。深色模式的对比度问题也走了"减卡片背景，增窗口背景"的非常规思路，避免改一堆硬编码白色。

### DDL 红线的"飘着画"

`QGridLayout` 单元格只能放整 widget，但红线/蓝线得按真实分钟落到任意像素点上。
做法：在课表网格上叠一层透明 overlay，**绝对坐标**摆线条。期间踩了三个坑（layout 还没跑完时坐标是 0×0、首次 show 没 resize 事件、定时器入口可能没触发 layout），都有针对性兜底，注释里写了"我们曾经因为这个 bug 调了半小时"。

### "现在"线无论几点都在

23:59 的 DDL、03:00 的"现在"——都不在课表的 8:00–21:30 范围内，但用户希望能看到位置。
我们用三段映射：早于第一节 → 顶端；晚于最后一节 → 底端；节间空隙 → 线性插值。21:31、03:00 这些边界全有测试覆盖。

---

## 工程数据

- **测试**：217 passed，覆盖业务逻辑、SQL、HTML 解析、AI 兜底、GUI 关键交互
- **测试策略**：业务逻辑层用手写 fake repo（快、不耦合 schema）；数据层跑真实 SQLite（验真实 SQL 行为）
- **本地优先**：用户数据全部 SQLite 本地存；AI、教学网都是按需可关
- **单一真源**：GUI 层只通过 `AppFacade` 调用业务，不直接访问数据库

---

## 项目地址

[github.com/BBBBBig-Spider/DDL-Center](https://github.com/BBBBBig-Spider/DDL-Center)
